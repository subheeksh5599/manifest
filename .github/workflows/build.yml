name: build

on: [push, pull_request]

jobs:
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: app/package-lock.json
      - run: npm ci
      - run: npm run build
      - run: npx tsc --noEmit

  program:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4
      - name: install solana
        shell: bash
        run: |
          # Pinned deliberately. An older release ships a cargo that cannot parse
          # the manifest of a transitive dependency, which failed this job on every
          # push for eight days while the program itself was fine.
          curl -sSfL https://release.anza.xyz/v3.1.15/install | sh
          echo "$HOME/.local/share/solana/install/active_release/bin" >> "$GITHUB_PATH"
      - name: build-sbf
        run: cargo build-sbf --manifest-path programs/preflight/Cargo.toml
      - name: test
        run: cargo test --manifest-path programs/preflight/Cargo.toml --lib